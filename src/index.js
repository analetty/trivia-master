import React from 'react';
import ReactDOM from 'react-dom';
import './css/App.css';
import App from './components/App';

// Muestra la App en el DOM dentro del elemento con id "root"
ReactDOM.render(<App />, document.getElementById('root'));
